# --- Agent (既存 agent.py が必要とするもの) ---
google-adk>=2.1.0
google-genai>=1.75.0   # main.pyがtypesを直接使用

# --- LLM本体 (Qwen / DashScope, LiteLLM経由) ---
# ADKにmodel=文字列を渡すとGeminiとして解釈されるため、Qwen等の非Geminiモデルは
# google.adk.models.lite_llm.LiteLlm でラップする必要がある。litellmはgoogle-adkに
# 同梱されない別パッケージなので明示的に追加。
# 注意: litellm 1.82.7 / 1.82.8 (2026-03-24 PyPI公開分) はサプライチェーン攻撃で
# 不正コードが混入した既知のインシデントあり(1.83.0で修正済み)。念のため新しめの
# バージョンに固定する。
litellm>=1.90.0

# mcp パッケージ（MongoDB公式MCP "mongodb-mcp-server" をnode直接実行で呼ぶために
# 必要。バンドルしたJS本体を起動するプロセス管理・stdio通信に使う。npxはv4で廃止）。
# 現行の安定版はv1.x系（最新1.28.1）で、v2は2026-07-28に安定版予定のプレリリース
# 中（2.0.0aN/bN）。pip/uvはデフォルトでプレリリースを選ばないが、念のため<2の
# 上限を付けて事故を防ぐ。
mcp>=1.27,<2
# --- Embedding (Qwen / DashScope OpenAI互換API) ---
# agent.py / 1_embed_videos.py の embed 呼び出しが openai SDK経由のQwenに
# 置き換わったため追加。
openai>=1.50.0
# --- Web backend ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
# pymongo Async API（AsyncMongoClient）を使用。4.9で導入、4.13で安定化。
# search_videos（$vectorSearchによる意味検索）のみこの依存で直結。
# find/count/list-collections/collection-schemaは公式MongoDB MCP経由
# （node直接実行、npxは不使用。下記の【JS側の依存】参照）。
pymongo[srv]>=4.13.0
# API利用制限
slowapi>=0.1.9

# =============================================================================
# 【JS側の依存・ビルド手順】mongodb-mcp-server（npm）
# =============================================================================
# pipとは別に、ビルド時に以下をbuild/へ実行してnode_modules/をcode.zipに含める。
# 実行にはFC側にNode.js 20公式公共層（nodeコマンド）が必要（npx/npmは不要）。
#
#   npm install --prefix build mongodb-mcp-server
#   rm -rf build/node_modules/@oven   # Bunランタイム用ネイティブバイナリ
#                                      # (4種×約85〜89MB=346MB)。mongodb-mcp-server
#                                      # 自体は使わないため除去（--help/--dryRunとも
#                                      # 削除後の動作を確認済み）。除去後は約135MB。
#
# 全体サイズ目安: Python依存(~40.5MB) + node_modules(~135MB) ≈ 176MB
# （FCのデプロイパッケージ上限500MBに対して十分余裕あり）
